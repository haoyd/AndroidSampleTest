<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhimazg.widgetdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.TestActivity1" />
        <activity android:name=".controller.activity.RequestActivity" />
        <activity android:name=".controller.activity.TitleTipActivity" />
        <activity android:name=".controller.activity.SpinnerActivity" />
        <activity android:name=".controller.activity.SpinnerActivity2"></activity>
    </application>

</manifest>